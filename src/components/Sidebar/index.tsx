import React, { ReactElement } from "react";
import styles from "./Sidebar.module.css";

const Sidebar: React.FC = (): ReactElement => {
  return <aside className={styles.sidebar}>guidebar</aside>;
};

export default Sidebar;
